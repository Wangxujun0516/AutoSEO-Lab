# CI Workflows (Future)
#
# Add your GitHub Actions workflows here when ready.
# Examples:
#   - Run SEO metadata validation on PR
#   - Auto-deploy docs/ to GitHub Pages
#   - Scheduled GSC data pull (via cron)
#
# File naming convention: *.yml (e.g., pages-deploy.yml, seo-audit.yml)
